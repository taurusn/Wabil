# dependencies
node_modules/

# secrets / runtime state
.env
.env.*
!.env.example
server/data/

# build artifacts (the web build is generated from app/ via `npm run build:web`)
app/dist/
app/web-build/

# logs / os
*.log
.DS_Store
.expo/

# test-instance isolated db (contains real inbox data — never commit)
server/data-test/
